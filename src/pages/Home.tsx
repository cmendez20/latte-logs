import { useEffect, useState } from "react";
import { createClient } from "@supabase/supabase-js";

// const supabase = createClient(

// );

export function Home() {
  return (
    <>
      <p className="text-blue-500">hello world this is home</p>
    </>
  );
}
